pages:
  image: openjdk
  script:
      - ls
      - javadoc ./src/* -classpath "./lib/*" -d public -quiet
  artifacts:
    paths:
      - public
    #- javadoc ...
# pages:
#     script:
#         — mkdir .public
#         — cp -r * .public
#         — mv .public public
#     artifacts:
#         paths:
#             [— public]
#     only:
#         [— master]