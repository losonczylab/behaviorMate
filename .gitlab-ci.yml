generate-doc:
  image: openjdk
  script:
      - ls ./..
      - ls
    #- javadoc ...
# pages:
#     script:
#         — mkdir .public
#         — cp -r * .public
#         — mv .public public
#     artifacts:
#         paths:
#             [— public]
#     only:
#         [— master]