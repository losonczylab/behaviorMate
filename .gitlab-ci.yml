generate-doc:
  image: openjdk
  script:
      - ls
      - javadoc -sourcepath "./src" -classpath "./lib/*"
    #- javadoc ...
# pages:
#     script:
#         — mkdir .public
#         — cp -r * .public
#         — mv .public public
#     artifacts:
#         paths:
#             [— public]
#     only:
#         [— master]